networks:
  shared-test-network:
    external: true # Reference the network created above

services:
  postgres:
    networks:
      - shared-test-network
  tracetest: 
    networks:
      - shared-test-network

